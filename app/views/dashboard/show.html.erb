<div class="w-full">
  <div class="flex justify-between items-center mb-8">
    <h1 class="font-bold text-4xl">Dashboard</h1>
  </div>

  <div class="grid grid-cols-2 gap-4">
    <%= render 'enrollments_by_course', data: @enrollments_by_course %>
    <%= render 'youngest_student_by_course', data: @youngest_student_by_course %>
    <%= render 'best_grades_by_subject', data: @best_grades_by_subject %>
    <%= render 'top_overloaded_teachers', data: @top_overloaded_teachers %>
  </div>
</div>
